# =============================================================================
# TMDB API Key (REQUIRED)
# =============================================================================
# Get your own FREE API key:
# 1. Create account: https://www.themoviedb.org/signup
# 2. Go to: https://www.themoviedb.org/settings/api
# 3. Request an API key (choose "Developer" option)
# 4. Copy the "API Key (v3 auth)" value below
TMDB_API_KEY=your_tmdb_api_key_here

# =============================================================================
# Database Configuration
# =============================================================================
# DEVELOPMENT (default): SQLite database file
# DATABASE_URL=sqlite:///./vose_movies.db

# PRODUCTION: PostgreSQL recommended for better performance and concurrency
# DATABASE_URL=postgresql://username:password@localhost:5432/vose_movies
# Or with connection string from hosting provider:
# DATABASE_URL=postgresql://user:pass@hostname:5432/dbname?sslmode=require

# =============================================================================
# API Server Configuration (Optional - defaults provided)
# =============================================================================
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=True

# =============================================================================
# Environment Mode
# =============================================================================
# Set to "production" for production deployments
# IMPORTANT: In production, you MUST set CORS_ORIGINS below
ENVIRONMENT=development

# =============================================================================
# CORS Configuration
# =============================================================================
# Comma-separated list of allowed origins (no spaces!)
# DEVELOPMENT: If not set, allows localhost, 127.0.0.1, and Android emulator origins
# PRODUCTION: If not set, CORS is DISABLED for security (you must configure it)
#
# Examples:
# CORS_ORIGINS=https://your-frontend-domain.com,https://app.your-domain.com
# CORS_ORIGINS=http://localhost:8081,http://10.0.2.2:8081

# =============================================================================
# Scraper Configuration (Optional)
# =============================================================================
SCRAPER_HEADLESS=True
SCRAPER_INTERVAL_HOURS=4
