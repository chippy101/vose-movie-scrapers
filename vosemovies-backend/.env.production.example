# =============================================================================
# PopcornPal Backend - Production Environment Configuration
# =============================================================================
# Copy this file to .env.production and configure for your production deployment
# On Render, set these in the dashboard Environment section instead

# =============================================================================
# Environment Mode
# =============================================================================
ENVIRONMENT=production

# =============================================================================
# Database Configuration (PostgreSQL - REQUIRED for production)
# =============================================================================
# Render automatically provides DATABASE_URL from the connected database
# Format: postgresql://user:password@hostname:5432/database?sslmode=require
# If deploying elsewhere, configure manually:
# DATABASE_URL=postgresql://username:password@hostname:5432/popcornpal

# =============================================================================
# TMDB API Key (REQUIRED)
# =============================================================================
# Get your own FREE API key:
# 1. Create account: https://www.themoviedb.org/signup
# 2. Go to: https://www.themoviedb.org/settings/api
# 3. Request an API key (choose "Developer" option)
# 4. Copy the "API Key (v3 auth)" value below
TMDB_API_KEY=your_production_tmdb_api_key_here

# =============================================================================
# API Server Configuration
# =============================================================================
API_HOST=0.0.0.0
# Render provides PORT automatically, but you can set default:
API_PORT=8000

# =============================================================================
# CORS Configuration (REQUIRED for production)
# =============================================================================
# Comma-separated list of allowed origins (NO SPACES!)
# IMPORTANT: Set this to your actual frontend domain(s)
CORS_ORIGINS=https://popcornpal.app,https://www.popcornpal.app

# Examples for different deployment scenarios:
# Single domain: CORS_ORIGINS=https://myapp.com
# Multiple domains: CORS_ORIGINS=https://myapp.com,https://www.myapp.com
# Expo app (if using): Add your Expo app URL

# =============================================================================
# Scraper Configuration
# =============================================================================
# Run scrapers in headless mode (no browser window)
SCRAPER_HEADLESS=True

# How often scrapers should run (in hours)
# Note: On Render, cron job schedule is in render.yaml, not here
SCRAPER_INTERVAL_HOURS=4

# =============================================================================
# Security Notes
# =============================================================================
# - NEVER commit this file with real credentials
# - Use Render dashboard Environment section to set secrets
# - DATABASE_URL is auto-provided by Render when database is connected
# - TMDB_API_KEY should be different from development key
# - CORS_ORIGINS must match your actual frontend domain exactly
# - All production URLs should use HTTPS, not HTTP
