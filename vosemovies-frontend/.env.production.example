# =============================================================================
# PopcornPal Frontend - Production Environment Configuration
# =============================================================================
# Copy this file to .env.production and configure for your production deployment

# =============================================================================
# TMDB API Configuration
# =============================================================================
# Get your own FREE API key:
# 1. Create account: https://www.themoviedb.org/signup
# 2. Go to: https://www.themoviedb.org/settings/api
# 3. Request an API key (choose "Developer" option)
# 4. Copy the "API Key (v3 auth)" value below
EXPO_PUBLIC_TMDB_API_KEY=your_production_tmdb_api_key_here

# =============================================================================
# Backend API Configuration (REQUIRED)
# =============================================================================
# Production backend URL - Replace with your actual Render deployment URL
EXPO_PUBLIC_BACKEND_URL=https://popcornpal-api.onrender.com

# Examples for different deployment scenarios:
# Render: EXPO_PUBLIC_BACKEND_URL=https://your-app-name.onrender.com
# Custom domain: EXPO_PUBLIC_BACKEND_URL=https://api.popcornpal.app
# Railway: EXPO_PUBLIC_BACKEND_URL=https://your-app.railway.app

# =============================================================================
# Build Configuration
# =============================================================================
# For production APK builds, ensure:
# 1. Backend URL points to your production server (HTTPS)
# 2. TMDB API key is valid and has sufficient rate limits
# 3. Backend CORS is configured to allow requests from your app

# =============================================================================
# Testing Production Build Locally
# =============================================================================
# Before deploying, test the production APK:
# 1. Set EXPO_PUBLIC_BACKEND_URL to your Render URL
# 2. Build APK: cd android && ./gradlew :app:assembleRelease
# 3. Install on device: adb install app/build/outputs/apk/release/app-release.apk
# 4. Test all features work with production backend

# =============================================================================
# Security Notes
# =============================================================================
# - NEVER commit this file with real credentials
# - Production backend URL must use HTTPS
# - Ensure backend CORS allows requests from your app
# - TMDB API key should be different from development key
# - Test thoroughly before distributing APK
